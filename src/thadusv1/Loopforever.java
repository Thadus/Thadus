/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package thadusv1;

/**
 *
 * @author netha
 */

/*
public class Loopforever {
    public static void loopforever(String response) {
        response = response.replace("loop", "").trim();
        response = response.replaceAll("\\(forever\\)", "").trim(); 


        while (true) {
            if (response.contains("print") && !response.contains(")") && !response.contains("(")) { 
                Print.print(response);
            }
            if (response.contains("print") && response.contains(")") && response.contains("(")) { 
                Printcalc.printcalc(response);
            }
            if (response.contains("num") && response.contains("=") && !response.contains("input")) {
                Num.num(response);   
            }
            if (response.contains("text") && response.contains("=") && !response.contains("input")) {
                Text.text(response);
            }
            if (response.contains("input") && response.contains("=")) {
                Input.input(response);  
            }  
        }
    }
}

*/