/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package thadusv1;

import java.util.ArrayList;
import java.util.List;

/**
 *
 * @author netha
 */

/*
public class Runfunction extends Function {
    public static void Runfunction (String response) {
        String functionname = response.replace("run function","").replace("(","").replace(")","").trim();
        List<String> functionruncode = new ArrayList<>();
        if (functionmap.containsKey(functionname)) {
            functionruncode = functionmap.get(functionname);
        }
        
        
        int i = 0;
        while (i < functionruncode.size()) {
        String runcode = (String) functionruncode.get(i);
        
        if (runcode.contains("print")&& !runcode.contains(")") && !runcode.contains("(") && !runcode.contains("loop") && !runcode.contains("if"))  { 
        Print.print(runcode);
        
        
        
        }
        
        
        if (runcode.contains("print")&& runcode.contains(")") && runcode.contains("(") && !runcode.contains("loop") && !runcode.contains("if"))  { 
        Printcalc.printcalc(runcode);
        
        
        }
        
        if (runcode.contains("num") && runcode.contains("=") && !runcode.contains("input")) {
            Num.num(runcode);
        
    }
        
        if (runcode.contains("text") && runcode.contains("=") && !runcode.contains("input")) {
            Text.text(runcode);
        }
        
        if (runcode.contains("input") && runcode.contains("=")) {
            Input.input(runcode);
            
        }
        
        
        
        
        
        




        
    }
    
}
}


*/